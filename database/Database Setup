CREATE TABLE customer_churn (
    customer_id INTEGER,
    churn INTEGER,
    age INTEGER,
    gender VARCHAR(10),
    marital_status VARCHAR(20),
    education_level VARCHAR(50),
    dependent_count INTEGER,
    occupation_type VARCHAR(50),
    annual_income INTEGER,
    income_band VARCHAR(50),
    income_category VARCHAR(50),
    city_tier VARCHAR(20),
    region VARCHAR(20),
    customer_segment VARCHAR(50),
    tenure_months INTEGER,
    onboarding_channel VARCHAR(50),
    relationship_type VARCHAR(50),
    number_of_products INTEGER,
    primary_account_type VARCHAR(50),
    card_category VARCHAR(20),
    customer_lifetime_value NUMERIC,
    loyalty_program_member INTEGER,
    referral_count INTEGER,
    last_contacted_days INTEGER,
    relationship_manager_assigned INTEGER,
    avg_monthly_balance NUMERIC,
    current_balance NUMERIC,
    balance_decline_percentage NUMERIC,
    monthly_transaction_count INTEGER,
    monthly_transaction_value NUMERIC,
    cash_withdrawal_count INTEGER,
    upi_transaction_count INTEGER,
    debit_card_transaction_count INTEGER,
    net_banking_transaction_count INTEGER,
    account_inactive_days INTEGER,
    total_trans_amt NUMERIC,
    total_trans_count INTEGER,
    total_amt_chng_q4_q1 NUMERIC,
    total_ct_chng_q4_q1 NUMERIC,
    avg_open_to_buy NUMERIC,
    total_revolving_bal NUMERIC,
    savings_account_flag INTEGER,
    current_account_flag INTEGER,
    credit_card_flag INTEGER,
    personal_loan_flag INTEGER,
    home_loan_flag INTEGER,
    auto_loan_flag INTEGER,
    fixed_deposit_flag INTEGER,
    investment_product_flag INTEGER,
    insurance_product_flag INTEGER,
    demat_account_flag INTEGER,
    credit_card_limit NUMERIC,
    credit_card_spend NUMERIC,
    credit_utilization_ratio NUMERIC,
    minimum_due_paid_flag INTEGER,
    late_credit_card_payment_count INTEGER,
    loan_outstanding_amount NUMERIC,
    emi_amount NUMERIC,
    emi_payment_delay_count INTEGER,
    loan_default_risk_score NUMERIC,
    prepayment_flag INTEGER,
    mobile_app_login_count INTEGER,
    website_login_count INTEGER,
    digital_transaction_ratio NUMERIC,
    last_login_days INTEGER,
    failed_login_count INTEGER,
    app_rating_given NUMERIC,
    paperless_statement_enabled INTEGER,
    digital_service_usage_score NUMERIC,
    mobile_banking_active_flag INTEGER,
    email_open_rate NUMERIC,
    total_complaints INTEGER,
    complaint_resolution_time NUMERIC,
    unresolved_complaint_count INTEGER,
    branch_visit_count INTEGER,
    call_center_interaction_count INTEGER,
    relationship_manager_interaction_count INTEGER,
    service_request_count INTEGER,
    escalation_count INTEGER,
    satisfaction_score NUMERIC,
    nps_score INTEGER,
    campaign_received_count INTEGER,
    campaign_response_count INTEGER,
    cross_sell_offer_count INTEGER,
    upsell_offer_count INTEGER,
    retention_offer_received INTEGER,
    retention_offer_accepted INTEGER,
    last_campaign_response_days INTEGER,
    discount_or_fee_waiver_received INTEGER,
    competitor_bank_offer_awareness VARCHAR(20),
    customer_feedback_sentiment VARCHAR(20),
    monthly_income_estimate NUMERIC,
    credit_utilization_3m_avg NUMERIC,
    credit_utilization_6m_avg NUMERIC,
    avg_quarterly_balance NUMERIC,
    total_digital_logins INTEGER,
    debt_to_income_ratio NUMERIC,
    digital_engagement_index NUMERIC
);

--Imported data from ChurnZero_dataset_v1.csv

SELECT *
FROM customer_churn;
